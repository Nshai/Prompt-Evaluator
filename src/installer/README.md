# Installer project

This folder contains a WiX-based installer definition for the AI Prompt Evaluator utility.

## Build

From the repository root, run:

```powershell
dotnet build installer\AiPromptEvaluator.wixproj
```

This produces an MSI package in the installer output folder.
